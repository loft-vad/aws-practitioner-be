service: authorization-service

frameworkVersion: "3"

plugins:
  - serverless-webpack
  - serverless-dotenv-plugin

useDotenv: true

provider:
  name: aws
  runtime: nodejs16.x
  stage: dev
  region: eu-central-1

functions:
  basicAuthorizer:
    handler: handlers.basicAuthorizer
